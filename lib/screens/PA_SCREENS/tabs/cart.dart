import 'package:flutter/material.dart';

class ShoppingCart1 extends StatelessWidget {
  const ShoppingCart1({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(10),
      child: const Text('Сагс'),
    );
  }
}
